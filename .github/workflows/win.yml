name: Windows RDP via localhost.run (No Signup)

on:
  workflow_dispatch:
    inputs:
      session_minutes:
        description: 'Duration in minutes'
        required: true
        default: '120'

jobs:
  rdp-tunnel:
    runs-on: windows-latest
    timeout-minutes: 360

    steps:
      - name: Enable RDP and create user
        shell: powershell
        run: |
          reg add "HKLM\System\CurrentControlSet\Control\Terminal Server" /v fDenyTSConnections /t REG_DWORD /d 0 /f
          netsh advfirewall firewall set rule group="Remote Desktop" new enable=yes
          reg add "HKLM\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp" /v UserAuthentication /t REG_DWORD /d 0 /f

          $username = "${{ secrets.RDP_USERNAME }}"
          $password = "${{ secrets.RDP_PASSWORD }}"

          if (Get-LocalUser -Name $username -ErrorAction SilentlyContinue) {
            Remove-LocalUser -Name $username
          }

          try {
            $securePass = ConvertTo-SecureString $password -AsPlainText -Force
            New-LocalUser -Name $username -Password $securePass -FullName "Remote Desktop User"
            Add-LocalGroupMember -Group "Administrators" -Member $username
            Add-LocalGroupMember -Group "Remote Desktop Users" -Member $username
            Write-Host "User $username created and RDP enabled."
          } catch {
            Write-Host "ERROR: $_"
            exit 1
          }

      - name: Start SSH reverse tunnel via localhost.run
        shell: powershell
        run: |
          # ایجاد فایل temporarily برای ذخیره خروجی
          $sshLog = "ssh.log"
          
          # اجرای SSH در بکگراند
          Start-Process -NoNewWindow -FilePath "ssh" `
            -ArgumentList "-o", "StrictHostKeyChecking=no", "-R", "0:localhost:3389", "nokey@localhost.run" `
            -RedirectStandardOutput $sshLog -RedirectStandardError $sshLog

          Write-Host "Waiting for tunnel to establish..."
          Start-Sleep -Seconds 10

          # استخراج IP و پورت از خروجی
          $tunnelInfo = $null
          for ($i=1; $i -le 10; $i++) {
            if (Test-Path $sshLog) {
              $content = Get-Content $sshLog -Raw
              # الگوی خروجی: "Connect to 1.2.3.4 port 54321"
              if ($content -match 'Connect to\s+([\d\.]+)\s+port\s+(\d+)') {
                $tunnelHost = $matches[1]
                $tunnelPort = $matches[2]
                $tunnelInfo = "$tunnelHost`:$tunnelPort"
                break
              }
            }
            Write-Host "Attempt $i/10..."
            Start-Sleep -Seconds 5
          }

          if (-not $tunnelInfo) {
            Write-Host "ERROR: Could not get tunnel info."
            if (Test-Path $sshLog) { Get-Content $sshLog }
            exit 1
          }

          @"
          host=$tunnelHost
          port=$tunnelPort
          "@ | Out-File -FilePath connection.txt -Encoding utf8

          Write-Host "Tunnel established."

      - name: Display connection information
        shell: powershell
        run: |
          $details = Get-Content connection.txt -Raw | ConvertFrom-StringData
          Write-Host ""
          Write-Host "======================================================"
          Write-Host "  WINDOWS RDP READY FOR ANDROID"
          Write-Host "======================================================"
          Write-Host "  Host: $($details.host)"
          Write-Host "  Port: $($details.port)"
          Write-Host "  Username: ${{ secrets.RDP_USERNAME }}"
          Write-Host "  Password: (your secret)"
          Write-Host "======================================================"
          Write-Host ""
          Write-Host "In Microsoft Remote Desktop on Android:"
          Write-Host "  PC Name: $($details.host):$($details.port)"
          Write-Host "  User Name: ${{ secrets.RDP_USERNAME }}"
          Write-Host ""

      - name: Keep session alive
        shell: powershell
        run: |
          $duration = [int]${{ github.event.inputs.session_minutes }}
          $endTime = (Get-Date).AddMinutes($duration)

          Write-Host "Session will stay active for $duration minutes."
          while ((Get-Date) -lt $endTime) {
            $remaining = ($endTime - (Get-Date)).ToString("hh\:mm\:ss")
            Write-Host "Remaining $remaining | $(Get-Date -Format 'HH:mm:ss')"

            # اگر SSH قطع شد، مجدداً اجرا شود
            if ((Get-Date).Minute % 5 -eq 0) {
              $sshProc = Get-Process -Name "ssh" -ErrorAction SilentlyContinue
              if (-not $sshProc) {
                Write-Host "SSH tunnel lost. Restarting..."
                Start-Process -NoNewWindow -FilePath "ssh" `
                  -ArgumentList "-o", "StrictHostKeyChecking=no", "-R", "0:localhost:3389", "nokey@localhost.run"
              }
            }
            Start-Sleep -Seconds 60
          }
          Write-Host "Time is up."